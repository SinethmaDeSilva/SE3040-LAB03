# SE3040-LAB03

## 🚀 Node.js Examples Implemented

### 1️⃣ Read File
Reads content from a text file using the `fs` module.

### 2️⃣ Write File
Writes text to a file using the `fs` module.

### 3️⃣ Create Web Server
Creates a simple HTTP server listening on port `8080`.

### 4️⃣ HTTP Request
Fetches data from an external API using the `https` module.

### 5️⃣ Using a Module
Demonstrates creating and importing a custom module.

### 6️⃣ Promises
Shows how to handle asynchronous operations using Promises.

### 7️⃣ Async / Await
Uses async/await syntax to handle Promises in a cleaner way.

> 📌 Each example was implemented in a **separate feature branch**, tested, pushed, merged via Pull Request, and pulled into the `main` branch.
